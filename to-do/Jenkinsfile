pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'python3 -m pytest --junit-xml test-reports/results.xml'
            }
            post {
                always {
                    junit 'test-reports/results.xml'
                }
            }

        }

    }
}
