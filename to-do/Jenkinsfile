pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'py.test --junit-xml test-reports/results.xml test ./tests'
            }
            post {
                always {
                    junit 'test-reports/results.xml'
                }
            }

        }

    }
}
